enum TypeSkill {
  flutter,
  rn,
  js,
  ts,
  php,
  git,
  googlePlay,
  appleStore,
  docker,
  aws
}

class SkillDescription {
  String title;
  String description;
  SkillDescription(this.title, this.description);
}
